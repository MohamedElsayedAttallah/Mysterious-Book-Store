<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Book - Mysterious Book Store</title>
  <link href="https://fonts.googleapis.com/css2?family=Cinzel&family=Lora&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
    <h1>Edit Book</h1>
  </header>

  <main>
    <section class="add-book">
      <h2>Edit Book Information</h2>
      <form action="/update" method="POST">
        <input type="hidden" name="bookId" value="<%= myArr.id %>">

        <label for="title">Title:</label>
        <input type="text" id="title" name="title" value="<%= myArr.title %>" required>
        
        <label for="rating">Rating (0 to 10):</label>
        <input type="number" id="rating" name="rating" value="<%= myArr.rating %>" min="0" max="10" step="0.01" required>

        <label for="notes">Notes:</label>
        <textarea id="notes" name="notes" required><%= myArr.notes %></textarea>

        <div style="display: flex; justify-content: space-between;">
          <button type="submit">Save</button>
          <a href="/" style="text-decoration: none;">
            <button type="button" style="background: #f44336; color: white;">Cancel</button>
          </a>
        </div>
      </form>
    </section>
  </main>
</body>
</html>
