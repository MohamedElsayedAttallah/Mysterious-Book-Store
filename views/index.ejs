<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Mysterious Book Store</title>
  <link href="https://fonts.googleapis.com/css2?family=Cinzel&family=Lora&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
    <h1>Mysterious Book Store</h1>
  </header>

  <main>
    <section class="add-book">
      <h2>Add a Book</h2>
      <form action="/add" method="POST">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" required>

        <label for="rating">Rating (0 to 10):</label>
        <input type="number" id="rating" name="rating" min="0" max="10" step="0.01" required>

        <label for="notes">Notes:</label>
        <textarea id="notes" name="notes" required></textarea>

        <button type="submit">Add Book</button>
      </form> 
    </section>

    <% if (flag) { %>
      <h2 style="text-align: center; font-size: xx-large; color: #d4af37; text-shadow: black;">Book Collection</h2>
      <% for (let item of myArr) { %>
        <section class="book-list">
          <div class="books">
            <div class="book-card" data-book-id="<%= item.id %>">
              <img src="<%= item.link %>" alt="Book Cover">
              <div class="info">
                <h3 id="title_<%= item.id %>" class="book-title"><%= item.title %></h3>
                <p class="book-rating">Rating: <%= item.rating %>/10</p>
                <p id="notes_<%= item.id %>" class="book-notes"><%= item.notes %></p>
              </div>
              <div class="actions">
                <form action="/edit" method="POST" id="edit_form_<%= item.id %>">
                  <input type="hidden" name="bookId" value="<%= item.id %>">
                  <button type="submit" class="edit-btn">Edit</button>
                </form>
                <form action="/remove" method="POST">
                  <input type="hidden" name="bookId" value="<%= item.id %>">
                  <button type="submit" class="delete-btn">Delete</button>
                </form>
              </div>
            </div>
          </div>
        </section>
      <% } %>
    <% } else { %>
      <h2 style="text-align: center;">Ready to make your film famous? Add it to our legendary book store!</h2> 
    <% } %>
  </main>
</body>
</html>
